server:
  servlet:
    encoding:
      # Servlet Response UTF-8 config
      charset: UTF-8
      force: true
    # Root Path config
    context-path: /stat
spring:
  jackson:
    serialization:
      fail-on-empty-beans: false

# Databricks JDBC
databricks:
    hostname: musinsa-data-ws.cloud.databricks.com
    port: 443
    http-path: /sql/1.0/warehouses/c0ee970a9c3ed562
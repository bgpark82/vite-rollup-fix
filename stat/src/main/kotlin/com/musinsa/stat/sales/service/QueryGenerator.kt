package com.musinsa.stat.sales.service

/**
 * 파라미터에 따라서 쿼리를 재생성 한다.
 */
object QueryGenerator {
    /**
     * 시작날짜와 종료날짜를 추가한다.
     *
     * @param orginQuery 원본 쿼리
     *
     * @return 시작날짜와 종료날짜가 추가된 쿼리.
     */
    fun addStarDateAndEndDate(orginQuery: String): String {
        return ""
    }
}